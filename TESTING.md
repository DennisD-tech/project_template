# TESTING.md

## 一页纸跑通测试（占位符）

### 前置条件

- 安装环境：Node.js / pnpm /（按项目需要填写）

### 标准测试命令（按项目实际命令调整）

- `pnpm install`
- `pnpm test`（或 `pnpm run test`）
- `pnpm run typecheck`（可选）
- `pnpm run build`（可选）

### 测试用例与命名约定

- 详见：`docs/06-quality/TESTING_STRATEGY.md`

